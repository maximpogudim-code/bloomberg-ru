# Необязательные тяжёлые зависимости — сайт работает и без них.
# faster-whisper — живая озвучка Bloomberg TV на русском
# yt-dlp        — получение потока Bloomberg TV
# playwright    — полные статьи Bloomberg (после установки: playwright install chromium)
faster-whisper==1.2.1
yt-dlp>=2025.1.1
playwright>=1.40.0
# imageio-ffmpeg — bundled ffmpeg binary; используется как фолбэк, если ffmpeg
# не установлен ни в PATH, ни в ~/bin (lazy import в live_audio._resolve_ffmpeg)
imageio-ffmpeg>=0.5
